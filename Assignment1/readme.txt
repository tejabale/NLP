The list of libraries along with versions we have used are:
pandas==1.4.3
numpy==1.24.3
nltk==3.8.1

In this assignment we have used the external package transphone for saperating the phonemes
pip install transphone

To test the new city name , you can use the streamlit GUI created using command 
streamlit run streamlit.py

